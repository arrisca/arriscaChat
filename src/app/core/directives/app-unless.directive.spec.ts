import {AppUnlessDirective} from './app-unless.directive';

describe('AppUnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new AppUnlessDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
